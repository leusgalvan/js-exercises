"use strict";

/*
Escribir un programa que:

- Defina una funcion llamada 'contiene' que reciba como parametros un arreglo y un numero.
  La funcion devuelve true si el arreglo contiene al elemento y false si no.
  Por ejemplo, si pasamos como parametros el arreglo [2, 3, 5] y el numero 3 la funcion
  devuelve true (porque el 3 esta en el arreglo), pero si pasamos el arreglo [2, 3, 5] y el numero 4
  la funcion devuelve false (porque el 4 no esta en el arreglo).
- Llame a la funcion con el arreglo [2, 4, 6] y el numero 6, y almacene el resultado en la variable 'resultado'
- Imprima el valor de la variable 'resultado' en consola.

Sugerencias:
- Se puede recorrer el arreglo con un bucle for
- Se pueden comparar valores con el operador ==
- Probar distintos valores para el arreglo y el numero
*/
